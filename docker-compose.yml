services:
  bot:
    build: .
    image: ghcr.io/zlobnoe/telegram_bot:latest
    restart: unless-stopped
    env_file: .env
    volumes:
      - bot-data:/data
      - ./credentials/google-credentials.json:/data/google-credentials.json:ro

volumes:
  bot-data:
